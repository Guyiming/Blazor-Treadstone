@page "/systemctl"
@inherits BasePage

<MRow>
    <MCol Sm="24" Lg="10">
        <MTextField Placeholder="english only" @bind-Value="@ServiceName" Prefix="服务名：" Suffix=".service">
        </MTextField>
    </MCol>

    <MCol Sm="24" Lg="14">
        <MTextField Placeholder="" @bind-Value="@ServiceDescription" Prefix="服务描述：">
        </MTextField>
    </MCol>

    <MCol Sm="24">
        <MTextField Placeholder="/usr/bin/dotnet /home/ubuntu/apps/Iot.SignalR/Iot.SignalR.dll" @bind-Value="@ExecCmd" Prefix="执行命令：">
        </MTextField>
    </MCol>


    <MCol Sm="24">
        <MTextField Placeholder="/home/ubuntu/apps/Iot.SignalR" @bind-Value="@WorkingDirectory" Prefix="设置执行命令的工作目录：">
        </MTextField>
    </MCol>
</MRow>
<br />
<br />


<MButton Depressed Color="primary" @onclick="@GenerateService" Block>生成</MButton>

<br />
<br />
<MRow>
    <MTextarea Outlined @bind-Value="@GenerateResult" style="height:300px;"></MTextarea>
</MRow>

<MDivider></MDivider>
<MRow>
    <MExpansionPanel>
        <MExpansionPanelHeader>
            后续操作
        </MExpansionPanelHeader>
        <MExpansionPanelContent>
            1.将生成的文本另存为@(ServiceName).serivce文件。
            2.将这个文件存放在/usr/lib/systemd/system目录下，并赋予754权限
            3.然后执行开机启动或者取消开机启动，systemctl enable/disable nginx.service
        </MExpansionPanelContent>
    </MExpansionPanel>
    @* <Collapse DefaultActiveKey='@(new[] { "1"})'>
        
    </Collapse> *@
</MRow>




